RCS = ReactionControl(15, 1.0, 0, 1, 0);

RCS.setCommands([0.01 0 0 0 2 0 0]);

for i = 1:1000
	RCS.tick(0.001);
[F, M] = RCS.getForceOnThisTick()
end

RCS.tick(0.001);
[F, M] = RCS.getForceOnThisTick()